# hello-morphospace Workflow State

Read in this order: `project.spec.json`, `feature.lock.json`,
`workspace.state.json`, the current or proposed unit, and referenced receipts.
This example is inert: `current_unit` is null and no feature is selected.
