# Static Admission Unexpected-Path Canary

This harmless extra path is intentional. The protected validation-workflow
change is approved only as an exact one-path change set, so static admission
must reject this expanded candidate path inventory.
